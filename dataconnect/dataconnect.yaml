specVersion: "v1beta"
serviceId: "vepa"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vepa-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
